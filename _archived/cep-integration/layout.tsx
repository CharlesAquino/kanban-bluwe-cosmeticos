"use client";
import { StandardPage } from "@/components/standard-page";

export default function CEPIntegrationLayout({ children }: { children: React.ReactNode }) {
  return (
    <StandardPage>{children}</StandardPage>
  );
}
